{
  "name": "fluxquint-trademark-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fluxquint™ — deterministic kinetic logic strategy game.",
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "test": "node scripts/build.mjs && node --test",
    "check": "node scripts/check.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
